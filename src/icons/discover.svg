<!-- Discover — Magnifying Glass with swooping entrance + discovery -->
<svg id="discover" viewBox="0 0 600 600" role="img" aria-labelledby="title desc"
     xmlns="http://www.w3.org/2000/svg"
     style="--start-angle: 45deg; --ink:#eaf0f7; --ring:#ffffff; --ring2:#ffffff; --line:#0f2031; --glass:#132231; --teal:#2ee6d6; --green:#7cffb0; --orange:#ffb36b; --violet:#b79cff; --gray:#cfd7e4; --shadow:#0a1522; --dot:#6db3d9; shape-rendering:geometricPrecision;">
  <title id="title">Discover</title>
  <desc id="desc">On hover (or data-active=true) the magnifier swoops in a circling arc and settles center; rim glints, lens wipes, dotted orbit draws, cubes jiggle then slow-spin, scan band passes, supplier hints reveal, and a “kr coin” deal pops into view with shine rays. Reduced-motion disables animation.</desc>

  <defs>
    <!-- soft outer glow -->
    <filter id="glow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="3.5" result="b"/>
      <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- subtle lens inner shadow -->
    <filter id="inset" x="-50%" y="-50%" width="200%" height="200%">
      <feOffset dx="0" dy="1"/>
      <feGaussianBlur stdDeviation="1.2" result="g"/>
      <feComposite in2="SourceAlpha" in="g" operator="arithmetic" k2="-1" k3="1"/>
    </filter>

    <!-- metal ferrule -->
    <linearGradient id="metal" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#f3f7fb"/>
      <stop offset=".55" stop-color="#dfe9f3"/>
      <stop offset="1" stop-color="#c9d6e3"/>
    </linearGradient>

    <!-- lens gloss -->
    <linearGradient id="gloss" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#fff" stop-opacity=".75"/>
      <stop offset=".6" stop-color="#fff" stop-opacity=".05"/>
      <stop offset="1" stop-color="#fff" stop-opacity="0"/>
    </linearGradient>

    <!-- wood -->
    <linearGradient id="wood" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0"  stop-color="#d9a96a"/>
      <stop offset=".5" stop-color="#bf8a53"/>
      <stop offset="1"  stop-color="#a9713e"/>
    </linearGradient>

    <!-- scan band gradient -->
    <linearGradient id="scanGrad" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0"   stop-color="#fff" stop-opacity="0"/>
      <stop offset=".45" stop-color="#fff" stop-opacity=".10"/>
      <stop offset=".5"  stop-color="#fff" stop-opacity=".18"/>
      <stop offset=".55" stop-color="#fff" stop-opacity=".10"/>
      <stop offset="1"   stop-color="#fff" stop-opacity="0"/>
    </linearGradient>

    <!-- motion trail gradient -->
    <linearGradient id="swooshGrad" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0"   stop-color="#fff" stop-opacity=".0"/>
      <stop offset=".35" stop-color="#fff" stop-opacity=".25"/>
      <stop offset=".6"  stop-color="#fff" stop-opacity=".55"/>
      <stop offset="1"   stop-color="#fff" stop-opacity="0"/>
    </linearGradient>

    <!-- coin fill -->
    <radialGradient id="coinGrad" cx="50%" cy="40%" r="60%">
      <stop offset="0%"  stop-color="#fff6cc"/>
      <stop offset="45%" stop-color="#f2d478"/>
      <stop offset="100%" stop-color="#c9a24b"/>
    </radialGradient>

    <!-- tiny square -->
    <symbol id="bit" viewBox="-2 -2 4 4"><rect x="-2" y="-2" width="4" height="4" rx=".6"/></symbol>

    <!-- cube base + variants -->
    <symbol id="cube-base" viewBox="-32 -32 64 64">
      <rect x="-22" y="-22" width="44" height="44" rx="9" fill="rgba(255,255,255,.06)" stroke="var(--ink)" stroke-width="1.5"/>
      <rect x="-22" y="-22" width="44" height="44" rx="9" fill="none" stroke="var(--dot)" stroke-opacity=".5" stroke-width="1"/>
    </symbol>

    <symbol id="cube-data" viewBox="-32 -32 64 64">
      <use href="#cube-base"/>
      <g stroke="var(--teal)" stroke-width="2" fill="none" opacity=".95">
        <rect x="-14" y="-10" width="28" height="18" rx="4"/>
        <path class="shim" d="M-12 -4h8v8m6-8v12m6-12h8m-26-8v4m-8 0h6m18 16h8"/>
      </g>
    </symbol>

    <symbol id="cube-inv" viewBox="-32 -32 64 64">
      <use href="#cube-base"/>
      <g stroke="var(--green)" stroke-width="1.8" fill="none" opacity=".95">
        <rect x="-14" y="-10" width="28" height="20" rx="4"/>
        <path d="M-14 0h28M0 -10v20"/>
        <circle cx="0" cy="0" r="10" opacity=".6"/>
      </g>
    </symbol>

    <symbol id="cube-neutral" viewBox="-32 -32 64 64">
      <use href="#cube-base"/>
      <rect x="-12" y="-12" width="24" height="24" rx="6" fill="rgba(255,255,255,.12)" stroke="var(--gray)" stroke-width="2"/>
    </symbol>

    <symbol id="cube-bits" viewBox="-32 -32 64 64">
      <use href="#cube-base"/>
      <rect x="-14" y="-10" width="28" height="20" rx="5" fill="rgba(255,255,255,.08)" stroke="var(--violet)" stroke-width="2"/>
      <g fill="var(--orange)">
        <use href="#bit" x="-6" y="-2"/><use href="#bit" x="-2" y="2"/>
        <use href="#bit" x="4"  y="-3"/><use href="#bit" x="8"  y="1"/>
        <use href="#bit" x="-9" y="4"/>
      </g>
    </symbol>

    <style>
      /* ---------- Core variables & base pose ---------- */
      :root { --start-angle: 45deg; --settle-deg: 14deg; --breath-deg: 2.6deg; }
      .ns{vector-effect:non-scaling-stroke}

      /* the whole rig flies the swoop path; inner .sweep keeps angular settle/breath */
      .rig{
        transform-origin:300px 300px;
        will-change: transform;
      }

      .sweep{
        transform-origin:300px 300px;
        transform: rotate(var(--start-angle));
        will-change: transform;
      }

      /* ---------- Hover triggers (or [data-active="true"]) ---------- */
      #discover:is(:hover, [data-hovered="true"]) .rig, #discover[data-active="true"] .rig{
        animation:
          swoopIn 1.05s cubic-bezier(.20,.85,.2,1) both,
          idleBob 4s 1.05s ease-in-out infinite;
      }

      #discover:is(:hover, [data-hovered="true"]) .sweep, #discover[data-active="true"] .sweep{
        animation:
          sweepOnce 1.2s .05s ease-in-out,
          hoverLock 3.2s 1.25s ease-in-out infinite;
      }

      /* looping arc-ish path into the center */
      @keyframes swoopIn{
        0%   { transform: translate(-220px,-220px) rotate(-18deg) scale(.94); }
        28%  { transform: translate(160px,-180px)  rotate(10deg)  scale(1.02); }
        58%  { transform: translate(210px,155px)   rotate(6deg)   }
        82%  { transform: translate(-120px,120px)  rotate(-4deg)  }
        100% { transform: translate(0,0)           rotate(0deg)   scale(1); }
      }
      @keyframes idleBob{
        0%{ transform:translate(0,0) }
        50%{ transform:translate(0,-2px) }
        100%{ transform:translate(0,0) }
      }

      @keyframes sweepOnce{
        0%   { transform: rotate(calc(var(--start-angle) - var(--settle-deg))) }
        55%  { transform: rotate(calc(var(--start-angle) + var(--settle-deg))) }
        100% { transform: rotate(var(--start-angle)) }
      }
      @keyframes hoverLock{
        0%   { transform: rotate(var(--start-angle)) }
        25%  { transform: rotate(calc(var(--start-angle) + var(--breath-deg))) }
        75%  { transform: rotate(calc(var(--start-angle) - var(--breath-deg))) }
        100% { transform: rotate(var(--start-angle)) }
      }

      /* ---------- Wipe reveal ---------- */
      .wipe{ transform-origin:300px 300px; transform:scale(1); }
      #discover:is(:hover, [data-hovered="true"]) .wipe, #discover[data-active="true"] .wipe{ animation:wipeIn 1.1s .30s ease-out both; }
      @keyframes wipeIn{ from{ transform:scale(0.4); } to{ transform:scale(1); } }

      /* ---------- Orbit draw (dotted) ---------- */
      .dash{ stroke-dasharray:4 8; stroke-dashoffset:260; }
      #discover:is(:hover, [data-hovered="true"]) .draw, #discover[data-active="true"] .draw{ animation: draw 1.1s .38s ease-out forwards; }
      @keyframes draw{ to{ stroke-dashoffset:0; } }

      /* ---------- Ring glint ---------- */
      .glint{ stroke:#fff; stroke-width:10; stroke-linecap:round; stroke-dasharray:70 1000; stroke-dashoffset:0; opacity:0; }
      #discover:is(:hover, [data-hovered="true"]) .glint, #discover[data-active="true"] .glint{ animation: glintRun 1s .28s ease-out both; }
      @keyframes glintRun{ 0%{opacity:.95;stroke-dashoffset:0} 100%{opacity:0;stroke-dashoffset:-520} }

      /* ---------- Motion trail (during swoop) ---------- */
      .swoosh{ opacity:0; transform-origin:300px 300px; }
      #discover:is(:hover, [data-hovered="true"]) .swoosh, #discover[data-active="true"] .swoosh{
        animation: trail 1.05s cubic-bezier(.2,.85,.2,1) both;
      }
      @keyframes trail{
        0%  { opacity:.85; stroke-dasharray:160 460; stroke-dashoffset:520; transform:scale(1.06) rotate(-10deg); }
        70% { opacity:.35; stroke-dashoffset:120; }
        100%{ opacity:0;   stroke-dashoffset:0;   transform:scale(1) rotate(0deg); }
      }

      /* ---------- Ground shadow shift ---------- */
      .ground{ transform-origin:300px 430px; opacity:.28; }
      #discover:is(:hover, [data-hovered="true"]) .ground, #discover[data-active="true"] .ground{ animation: shadowSweep 1.2s ease-in-out both; }
      @keyframes shadowSweep{
        0%{transform:scaleX(1);opacity:.28}
        55%{transform:scaleX(1.18);opacity:.18}
        100%{transform:scaleX(1);opacity:.28}
      }

      /* ---------- Lens parallax (subtle depth) ---------- */
      .parallax{ transform-origin:300px 300px; opacity:1; }
      #discover:is(:hover, [data-hovered="true"]) .parallax, #discover[data-active="true"] .parallax{
        animation: parallax 1s .10s ease-out both,
                   parallaxBreath 3.2s 1.25s ease-in-out infinite,
                   lensReveal 1.05s cubic-bezier(.20,.85,.2,1) both;
      }
      @keyframes parallax{
        0%{   transform:translate(-18px,-12px) }
        80%{  transform:translate(  6px,  4px) }
        100%{ transform:translate(  0px,  0px) }
      }
      @keyframes parallaxBreath{
        0%{transform:translate(0,0)}
        50%{transform:translate(-1px,1px)}
        100%{transform:translate(0,0)}
      }
      @keyframes lensReveal{
        0%, 94%{ opacity:0; }
        100%  { opacity:1; }
      }

      /* ---------- Cubes: jiggle + arrival tick + group spin ---------- */
      .cube{ transform-box:fill-box; transform-origin:center; }
      #discover:is(:hover, [data-hovered="true"]) .jig, #discover[data-active="true"] .jig{ animation:jig .5s .58s cubic-bezier(.2,.9,.2,1) both; }
      @keyframes jig{0%{transform:translate(0,0) scale(1)}40%{transform:translate(0,-2px) scale(1.06)}100%{transform:translate(0,0) scale(1)}}
      #discover:is(:hover, [data-hovered="true"]) .spinKick, #discover[data-active="true"] .spinKick{ animation:kick .14s 1.08s cubic-bezier(.2,.9,.2,1) both; }
      @keyframes kick{from{transform:scale(0.985)}to{transform:scale(1)}}
      .cubesSpin{ transform-origin:300px 300px; }
      #discover:is(:hover, [data-hovered="true"]) .spin, #discover[data-active="true"] .spin{ animation: spin 16s 1.12s linear infinite; }
      @keyframes spin{ to{ transform:rotate(360deg); } }

      /* ---------- Cube ping rings ---------- */
      .ping{ fill:none; stroke:var(--ring); stroke-opacity:.55; }
      #discover:is(:hover, [data-hovered="true"]) .ping, #discover[data-active="true"] .ping{ animation: ping 1.6s 0.62s ease-out infinite; }
      @keyframes ping{ 0%{opacity:0;r:18} 15%{opacity:.8;r:22} 100%{opacity:0;r:36} }

      /* ---------- Data shimmer on cube ---------- */
      .shim{ stroke-dasharray:3 6; }
      #discover:is(:hover, [data-hovered="true"]) .shim, #discover[data-active="true"] .shim { animation: shim 3.6s linear infinite; }
      @keyframes shim{ to{ stroke-dashoffset:-36 } }

      /* ---------- Scan pass + discoverables ---------- */
      .scanBand{ opacity:0; transform-origin:300px 300px; }
      #discover:is(:hover, [data-hovered="true"]) .scanBand, #discover[data-active="true"] .scanBand{
        animation: scanPass .9s .38s ease-out both;
      }
      @keyframes scanPass{
        0%  { opacity:0; transform:translateX(-160px) }
        15% { opacity:.35; }
        85% { opacity:.35; }
        100%{ opacity:0; transform:translateX(160px) }
      }

      .disc{ opacity:0; transform-origin:center; }
      #discover:is(:hover, [data-hovered="true"]) .d1, #discover[data-active="true"] .d1{ animation: discIn .35s .50s cubic-bezier(.2,.9,.2,1) both; }
      #discover:is(:hover, [data-hovered="true"]) .d2, #discover[data-active="true"] .d2{ animation: discIn .35s .56s cubic-bezier(.2,.9,.2,1) both; }
      #discover:is(:hover, [data-hovered="true"]) .d3, #discover[data-active="true"] .d3{ animation: discIn .35s .62s cubic-bezier(.2,.9,.2,1) both; }
      #discover:is(:hover, [data-hovered="true"]) .d4, #discover[data-active="true"] .d4{ animation: discIn .35s .68s cubic-bezier(.2,.9,.2,1) both; }
      @keyframes discIn{
        0%{ opacity:0; transform:translateY(6px) scale(.96) }
        60%{ opacity:1; transform:translateY(-1px) scale(1.04) }
        100%{ opacity:1; transform:translateY(0) scale(1) }
      }

      /* ---------- Discovery badge (kr coin + shine) ---------- */
      .find{ opacity:0; transform-origin:300px 300px; }
      #discover:is(:hover, [data-hovered="true"]) .find, #discover[data-active="true"] .find{
        animation: popIn .42s .64s cubic-bezier(.2,.9,.2,1) both;
      }
      @keyframes popIn{
        0%{ opacity:0; transform:translateY(6px) scale(.7) rotate(-6deg) }
        80%{ opacity:1; transform:translateY(-1px) scale(1.06) rotate(0deg) }
        100%{ opacity:1; transform:translateY(0) scale(1) }
      }
      .rays line{ stroke:#fff; stroke-opacity:.9; stroke-linecap:round; }
      #discover:is(:hover, [data-hovered="true"]) .rays line, #discover[data-active="true"] .rays line{
        animation: ray 1.2s .68s ease-out both;
      }
      @keyframes ray{
        0%{ opacity:0; transform:scale(.8); }
        35%{ opacity:1; }
        100%{ opacity:0; transform:scale(1.15); }
      }

      /* Motion safety */
      @media (prefers-reduced-motion:reduce){
        *{ animation:none !important; }
        .parallax{ opacity:1 !important; }
      }
    </style>
  </defs>

  <!-- confetti (static) -->
  <g fill="var(--dot)" opacity=".65">
    <rect x="129" y="140" width="4" height="4" rx=".6"/>
    <rect x="466" y="166" width="4" height="4" rx=".6"/>
    <rect x="492" y="412" width="4" height="4" rx=".6"/>
    <rect x="140" y="417" width="4" height="4" rx=".6"/>
    <rect x="183" y="457" width="3" height="3" rx=".5"/>
    <rect x="525" y="220" width="3" height="3" rx=".5"/>
  </g>

  <!-- ===== WHOLE RIG (flies in a loop) ===== -->
  <g class="rig">

    <!-- motion swoosh trail -->
    <circle class="swoosh" cx="300" cy="300" r="188" fill="none" stroke="url(#swooshGrad)" stroke-width="10"/>

    <!-- ground shadow (moves with rig) -->
    <ellipse class="ground" cx="300" cy="430" rx="120" ry="24" fill="var(--shadow)"/>

    <!-- ===== MAGNIFYING GLASS (rotates as one .sweep) ===== -->
    <g class="sweep">

      <!-- glass (unchanged) -->
      <circle cx="300" cy="300" r="145" fill="var(--glass)" filter="url(#inset)"/>
      <!-- gloss arcs -->
      <path d="M230 240a111 111 0 0 1 49-37" stroke="url(#gloss)" stroke-width="14" fill="none" opacity=".55"/>
      <path d="M387 387a111 111 0 0 0 44-35" stroke="url(#gloss)" stroke-width="12" fill="none" opacity=".35"/>

      <!-- ring -->
      <g filter="url(#glow)">
        <circle cx="300" cy="300" r="164" fill="none" stroke="var(--ring2)" stroke-width="12" opacity="1"/>
        <circle cx="300" cy="300" r="155" fill="none" stroke="var(--ring)"  stroke-width="7"  opacity="1"/>
      </g>

      <!-- rim glint -->
      <circle class="glint" cx="300" cy="300" r="160" fill="none"/>

      <!-- handle anchored at ring’s rightmost (16:30 orientation) -->
      <g transform="translate(464,300)">
        <!-- ferrule -->
        <rect x="-4" y="-16" width="26" height="32" rx="10" fill="url(#metal)" stroke="#c1ccd8" stroke-width="1.6"/>
        <rect x="-2" y="-13" width="22" height="26" rx="9" fill="none" stroke="#9fb0bf" stroke-width="1.1" opacity=".8"/>
        <!-- wooden handle -->
        <rect x="20" y="-14" width="150" height="28" rx="14" fill="url(#wood)" stroke="#7a532f" stroke-width="1.8"/>
        <g stroke="#7a532f" stroke-opacity=".35" fill="none">
          <path d="M32 -6 q10 6 0 12 t0 12 t0 12"/>
          <path d="M60 -7 q-8 6 0 12 t0 12 t0 12"/>
          <path d="M92 -5 q3 6 -3 12 t3 12 t-3 12"/>
        </g>
        <ellipse cx="170" cy="0" rx="14" ry="18" fill="#9b6a3b" stroke="#7a532f" stroke-width="1.6"/>
      </g>

      <!-- ===== LENS CONTENT (parallax, masked by wipe) ===== -->
      <g class="parallax" mask="url(#wipeMask)">
        <!-- dotted orbit -->
        <path class="dash draw" d="M300,192 a108,108 0 1,1 0,216 a108,108 0 1,1 0,-216"
              stroke="var(--ink)" stroke-width="2.6" fill="none" opacity=".85"/>

        <!-- scan band (soft light sweep across the lens) -->
        <rect class="scanBand" x="120" y="120" width="360" height="360" fill="url(#scanGrad)"/>

        <!-- discoverables: supplier/wholesale hints -->
        <g>
          <!-- pallet -->
          <g class="disc d1" transform="translate(245,380)">
            <g fill="none" stroke="var(--ink)" stroke-width="2">
              <rect x="-26" y="-16" width="52" height="20" rx="4" fill="rgba(255,255,255,.05)"/>
              <path d="M-18 0h36M-26 8h8m10 0h10m10 0h8"/>
            </g>
          </g>
          <!-- barcode -->
          <g class="disc d2" transform="translate(345,372)">
            <g fill="var(--ink)">
              <rect x="-26" y="-10" width="2" height="20" opacity=".9"/>
              <rect x="-22" y="-10" width="4" height="20" opacity=".7"/>
              <rect x="-16" y="-10" width="3" height="20" opacity=".85"/>
              <rect x="-10" y="-10" width="6" height="20" opacity=".6"/>
              <rect x="-2"  y="-10" width="2" height="20" opacity=".9"/>
              <rect x="4"   y="-10" width="5" height="20" opacity=".75"/>
              <rect x="12"  y="-10" width="3" height="20" opacity=".88"/>
              <rect x="18"  y="-10" width="2" height="20" opacity=".65"/>
            </g>
          </g>
          <!-- price tag -->
          <g class="disc d3" transform="translate(232,240) rotate(-20)">
            <g fill="none" stroke="var(--ink)" stroke-width="2">
              <path d="M-18 -6 L8 -6 L18 4 L-8 4 Z" fill="rgba(255,255,255,.06)"/>
              <circle cx="-12" cy="-1" r="2.5" fill="var(--ink)"/>
            </g>
          </g>
          <!-- small truck -->
          <g class="disc d4" transform="translate(362,250)">
            <g fill="none" stroke="var(--ink)" stroke-width="2">
              <rect x="-18" y="-10" width="20" height="14" rx="2" fill="rgba(255,255,255,.05)"/>
              <rect x="2" y="-10" width="14" height="10" rx="2"/>
              <circle cx="-10" cy="6" r="4" fill="var(--ink)"/>
              <circle cx="8"  cy="6" r="4" fill="var(--ink)"/>
            </g>
          </g>
        </g>

        <!-- four cubes (cardinals) -->
        <g class="cubesSpin spin">
          <g transform="translate(300,192)">
            <circle class="ping" r="18"/>
            <g class="cube jig spinKick"><use href="#cube-data" width="52" height="52" x="-26" y="-26"/></g>
          </g>
          <g transform="translate(408,300)">
            <circle class="ping" r="18"/>
            <g class="cube jig spinKick"><use href="#cube-inv" width="52" height="52" x="-26" y="-26"/></g>
          </g>
          <g transform="translate(300,408)">
            <circle class="ping" r="18"/>
            <g class="cube jig spinKick"><use href="#cube-bits" width="52" height="52" x="-26" y="-26"/></g>
          </g>
          <g transform="translate(192,300)">
            <circle class="ping" r="18"/>
            <g class="cube jig spinKick"><use href="#cube-neutral" width="52" height="52" x="-26" y="-26"/></g>
          </g>
        </g>

        <!-- ===== Discovery object: "kr coin" + shine rays ===== -->
        <g class="find">
          <!-- coin -->
          <g transform="translate(300,300)">
            <circle r="30" fill="url(#coinGrad)" stroke="#8f6b2a" stroke-width="2"/>
            <circle r="24" fill="none" stroke="#d8b35c" stroke-width="2" opacity=".8"/>
            <!-- subtle coin texture -->
            <g stroke="#e8c974" stroke-opacity=".45">
              <path d="M-18 0h36M-16 -6h32M-16 6h32" />
            </g>
            <!-- tiny 'kr' mark (no currency sign) -->
            <g fill="var(--line)" opacity=".85" transform="translate(-8,6) scale(1.05)">
              <rect x="-2" y="-8" width="4" height="16" rx="1"/>
              <path d="M4,-1 l10,-9 M4,1 l10,9" stroke="var(--line)" stroke-width="3" stroke-linecap="round" fill="none"/>
            </g>
          </g>
          <!-- shine rays -->
          <g class="rays" transform="translate(300,300)">
            <line x1="0" y1="-44" x2="0" y2="-60" stroke-width="3"/>
            <line x1="31" y1="-31" x2="43" y2="-43" stroke-width="3"/>
            <line x1="44" y1="0"   x2="60" y2="0"   stroke-width="3"/>
            <line x1="31" y1="31"  x2="43" y2="43"  stroke-width="3"/>
            <line x1="0" y1="44"   x2="0" y2="60"   stroke-width="3"/>
            <line x1="-31" y1="31" x2="-43" y2="43" stroke-width="3"/>
            <line x1="-44" y1="0"  x2="-60" y2="0"  stroke-width="3"/>
            <line x1="-31" y1="-31" x2="-43" y2="-43" stroke-width="3"/>
          </g>
        </g>
      </g>
    </g>
  </g>

  <!-- reveal mask (kept last for clarity; uses the same rect as before) -->
  <mask id="wipeMask">
    <rect width="600" height="600" fill="black"/>
    <rect class="wipe" x="120" y="120" width="360" height="360" rx="140" ry="140" fill="white"/>
  </mask>
</svg>
