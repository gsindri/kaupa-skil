{
  "name": "sync-price-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "zip": "npm run build && cd dist && zip -r ../EXT_NAME-dist.zip ."
  },
  "devDependencies": {
    "esbuild": "^0.19.12",
    "typescript": "^5.8.3"
  }
}
